# 路线

## 第一版

走通闭环：

- 自动路由选定会话模型。
- 自动推理强度在允许的档位里选最低够用的一档，不参考上一轮。
- 指定模型加自动推理强度只重选推理强度。
- 指定模型和指定推理强度保持不变。
- 路由失败时，这次模型请求失败；推理强度连续 HTTP 503 的故障降级除外。
- 只在主请求上问 Jev。旁路请求不问。
- 模型列表提供 `auto/jev`。推理强度列表提供「自动」。
- 选会话模型时只带用户和助手正文。选推理强度时可以再带工具结果摘录。不带系统提示、工具参数和图片。放不下就路由失败。
- 只剩一个可用选项时直接用它，不问 Jev。一个都没有就路由失败。
- 自动模型及其任务说明、各模型的推理档位说明由部署配置提供，模型 ID 不限制为固定版本；只有已注册且支持当前输入的模型和档位可选。
- 第一次主请求先问会话模型，再问推理强度。之后的主请求只问推理强度。
- 同一会话里再次选中 `auto/jev` 后，下一次主请求重新选会话模型。只改任务说明不重选。
- 子会话不另选会话模型，沿用父会话已经选定的具体模型。推理强度跟随父会话的选择方式。
- 会话模型只在模型选择和当次推理强度确定后留下；连续 HTTP 503 可用同模型上一档或适配器默认档确定强度。其他强度失败不取消已有会话模型。
- 一次评估最多最近 8 条；工具结果摘录每条 1600 字；请求体 28000 字节。每次尝试超时 5 秒，HTTP 503 间隔 1 秒最多重试两次，总上限 17 秒；其他错误不重试。

## 以后

这些来自 pi-jev-router，第一版不做：

- 模型监控：发现更合适的模型时只建议另开会话，不自动更换。
- 自动加载 skill。
- 长提示分块：单次评估放不下时拆段再合并。
- HTTP 503 之外的重试、超过两次重试和跨评估共享超时预算。
- 查看当前选择、建议和评估费用。
- 推理强度下限。
- 结构化的任务说明（角色、适用、不适用、边界）。

## 明确不做

- 不设备用模型。选模型失败就是这次请求失败。
- 正常评估不把上一轮推理强度当作保持理由；仅连续 HTTP 503 的故障降级沿用上一档。
