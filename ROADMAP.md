# 路线

## 第一版

走通闭环：

- 自动路由选定会话模型。
- 自动推理强度在允许的档位里选最低够用的一档，不参考上一轮。可按模型配置下限，只提供下限及以上的档位；指定推理强度不受下限影响。
- 指定模型加自动推理强度只重选推理强度。
- 指定模型和指定推理强度保持不变。
- 路由失败时，这次模型请求失败；推理强度连续临时故障的降级除外。
- 只在主请求上问 Jev。旁路请求不问。
- 模型列表提供 `auto/jev`。推理强度列表提供「自动」。
- 选会话模型时只带用户和助手正文。选推理强度时可以再带工具结果摘录。不带系统提示、工具参数和图片。放不下时先丢较早的消息，最新用户消息始终保留；只剩它仍放不下就保留首尾、中间标注省略字数；连这样也放不下（如选项说明过长）才路由失败。
- 只剩一个可用选项时直接用它，不问 Jev。一个都没有就路由失败。
- 自动模型及其任务说明、各模型的推理档位说明由部署配置提供，模型 ID 不限制为固定版本；只有已注册且支持当前输入的模型和档位可选。
- 第一次主请求先问会话模型，再问推理强度。之后的主请求只问推理强度。
- 同一会话里再次选中 `auto/jev` 后，下一次主请求重新选会话模型。只改任务说明不重选。
- 子会话不另选会话模型，沿用父会话已经选定的具体模型。推理强度跟随父会话的选择方式。
- 会话模型只在模型选择和当次推理强度确定后留下；连续临时故障可用同模型上一档或适配器默认档确定强度。其他强度失败不取消已有会话模型。
- 一次评估最多最近 8 条；工具结果摘录每条 1600 字；请求体 28000 字节。每次尝试超时 5 秒；临时故障（HTTP 408/429/500/502/503/504、网络错误、单次超时）间隔 1 秒最多重试两次，总上限 17 秒；其他错误和凭证读取超时不重试。
- 每个会话的评估结果、用量和网关费用记入私有 ledger；`jev-router-inspect` skill 按需查看会话选择、档位分布和费用，并可问 Jev 是否另开会话换模型。只建议，不自动更换，也不在后台监控。

## 以后

这些来自 pi-jev-router，第一版不做：

- 自动加载 skill。
- 超过两次重试和跨评估共享超时预算。
- 结构化的任务说明（角色、适用、不适用、边界）。

## 明确不做

- 不设备用模型。选模型失败就是这次请求失败。
- 正常评估不把上一轮推理强度当作保持理由；仅连续临时故障的降级沿用上一档。
- 不做长提示分块合并。单条超长消息拆开后，片段往往看不到用户的问题，模型选择也没有合理的合并规则，而推理强度每步都要评估，分块会让费用和耗时成倍增加。改用丢弃较早消息、截断最新用户消息中间部分的方式。
