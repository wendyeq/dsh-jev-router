# Jev 路由

DeepSeek Harness 里由 Jev 决定模型和推理强度的会话选择。

## Language

**Jev**:
官方的 TypeSafe 选择模型。ask-jev 调用的就是它，不是另一个模型。
_Avoid_: skill 内嵌的 Jev、另一套 Jev

**自动路由**:
用户以 `auto/jev` 进入的选择。模型是会话模型，推理强度是自动推理强度。
_Avoid_: 自动模式、Jev 模式

**会话模型**:
自动路由里，Jev 为一个会话选定并在该会话内保持不变的模型。它在当次推理强度也选定之后才成立。用户另开的会话另行选择。同一会话里再次选中自动路由时，原来的会话模型作废。
_Avoid_: 每轮重选的模型

**子会话**:
父会话之下另开的会话。它不自行选择会话模型；未指定模型时使用父会话已经选定的具体模型。
_Avoid_: 另一个会话、fork

**指定模型**:
用户选定的具体模型。Jev 不更换它。
_Avoid_: 手动模型、钉选模型

**自动推理强度**:
未指定具体档位的推理强度。每一次主请求由 Jev 单独决定档位。
_Avoid_: 自适应推理、钉住的强度

**主请求**:
属于当前会话本身的模型请求，包括用户消息和随后的工具来回。
_Avoid_: 全部模型调用

**旁路请求**:
不属于当前会话本身的模型请求，例如会话标题和压缩。它不询问 Jev，也不选定会话模型。
_Avoid_: 辅助请求、auxiliary

**指定推理强度**:
用户选定的具体推理强度。Jev 不更换它。
_Avoid_: 固定 thinking、手动 effort

**自动模型**:
允许进入自动路由的模型。当前是 GPT-6 Astra、GPT-6 Sol、GPT-6 Luna。
_Avoid_: 路由候选、全部已登录模型

**路由失败**:
Jev 没能在上限内给出一个允许的选择。这次模型请求失败，不改用其他模型。推理强度的降级除外。
_Avoid_: 备用模型、降级

**降级**:
推理强度评估连续临时故障后，不问 Jev，改用同一模型上一次的档位，没有时用适配器默认档；低于下限时取下限。只用于推理强度，选模型失败仍是路由失败。
_Avoid_: 备用模型、路由失败
